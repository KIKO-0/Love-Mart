<wxs module="util" src="../util.wxs" />

<view class="mission-dashboard">
  <view class="dashboard-title">MISSION CONTROL</view>
  <view class="dashboard-stats">
    <view class="stat-item">
      <view class="stat-num">{{unfinishedMissions.length}}</view>
      <view class="stat-label">Pending</view>
    </view>
    <view class="divider"></view>
    <view class="stat-item">
      <view class="stat-num">{{finishedMissions.length}}</view>
      <view class="stat-label">Done</view>
    </view>
  </view>
</view>

<view class="container white" wx:if="{{allMissions.length === 0}}">
  <image class="background" src="Images/None.png" mode="aspectFit" />
  <view class="title">暂无任务</view>
  <view class="subtitle">好像什么任务也没有...</view>
</view>

<view class="container" wx:if="{{allMissions.length > 0}}">
  <view class="form-group"><view class="search-bar">
    <icon class="search-icon" type="search" size="24"></icon>
    <input class="search-input" placeholder="搜索任务" bindinput="onSearch" value="{{search}}" />
  </view></view>

  <view class="form-group_label">未完成<mp-badge content="{{unfinishedMissions.length}}" style="margin-left: 5px;" /></view>
  <view class="form-group" wx:if="{{unfinishedMissions.length > 0}}">
    <view class="form-cell" wx:for="{{unfinishedMissions}}" wx:key="index">
      <mp-slideview buttons="{{slideButtons}}" data-index='{{index}}' icon="{{true}}" bindbuttontap="slideButtonTapUpper">
        <view class="slide" data-index="{{index}}" bindtap="toDetailPageUpper">
          <image class="author-avatar" wx:if="{{item._openid === _openidA}}" mode="aspectFill" src="/images/AvatarA.png"/>
          <image class="author-avatar" wx:if="{{item._openid === _openidB}}" mode="aspectFill" src="/images/AvatarB.png"/>
          
          <view class="mission-info">
            <view class="mission-info_title">{{util.filterStr(item.title, 10)}}</view>
            <view class="mission-info_date">📅 {{util.cutStr(item.date, 10)}}</view>
          </view>
          
          <view class="mission-right">
             <view class="credit-tag">💰 {{util.filterStr(item.credit, 10)}}</view>
             <image wx:if="{{item.star}}" class="star" src="Images/Star.png" />
          </view>
        </view>
      </mp-slideview>
    </view>
  </view>

  <view class="form-group_label">已完成<mp-badge content="{{finishedMissions.length}}" style="margin-left: 5px;" /></view>
  <view class="form-group" wx:if="{{finishedMissions.length > 0}}">
    <view class="form-cell" wx:for="{{finishedMissions}}" wx:key="index">
      <mp-slideview buttons="{{slideButtons}}" data-index='{{index}}' icon="{{true}}" bindbuttontap="slideButtonTapLower">
        <view class="slide" data-index="{{index}}" bindtap="toDetailPageLower">
          <image class="author-avatar" wx:if="{{item._openid === _openidA}}" mode="aspectFill" src="/images/AvatarA.png"/>
          <image class="author-avatar" wx:if="{{item._openid === _openidB}}" mode="aspectFill" src="/images/AvatarB.png"/>

          <view class="mission-info">
            <view class="mission-info_title">{{util.filterStr(item.title, 10)}}</view>
            <view class="mission-info_date">📅 {{util.cutStr(item.date, 10)}}</view>
          </view>
          
          <view class="mission-right">
             <view class="credit-tag done">💰 {{util.filterStr(item.credit, 10)}}</view>
             <image wx:if="{{item.star}}" class="star" src="Images/Star.png" />
          </view>
        </view>
      </mp-slideview>
    </view>
  </view>
</view>

<movable-area style="pointer-events: none; position: fixed; width: 100%; height: 100%; left: 0; top: 0; z-index: 999;">
  <movable-view style="pointer-events: auto; width: 140px; height: 50px;" x="{{xPosition}}" y="{{yPosition}}" direction="all">
    <button class="btn-normal" hover-class="btn-hover" bindtap="toAddPage">
      <image class="btn-icon" src="Images/Plus.png" mode="widthFix" />
      <text class="btn-text">添加任务</text>
    </button>
  </movable-view>
</movable-area>